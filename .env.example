# SendGrid configuration (for contact form)
SENDGRID_API_KEY=your_sendgrid_api_key_here
CONTACT_TO_EMAIL=your_email@example.com
 
# Google Sheets (optional)
# Provide a service account JSON (either paste the JSON or base64-encode it)
# For platforms like Vercel you can copy the full JSON string, or use base64 to avoid multiline issues.
GOOGLE_SHEET_ID=your_google_sheet_id_here
# Example: set GOOGLE_SERVICE_ACCOUNT=$(cat service-account.json | base64)
GOOGLE_SERVICE_ACCOUNT=base64_or_json_service_account_here
